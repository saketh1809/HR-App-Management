apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongodb-hr-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mongodb-hr-deployment
  template:
    metadata:
      labels:
        app: mongodb-hr-deployment
    spec:
      containers:
      - name: mongodb-hr-deployment
        image: mongo:6.0
        ports:
        - containerPort: 27017
        volumeMounts:
        - name: mongodb-storage
          mountPath: /data/db
      volumes:
      - name: mongodb-storage
        emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: mongodb-hr-service
spec:
  selector:
    app: mongodb-hr-deployment
  ports:
  - port: 27017
    targetPort: 27017
  type: ClusterIP
 
 